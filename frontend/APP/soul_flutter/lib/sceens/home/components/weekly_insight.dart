import 'package:flutter/material.dart';

class WeeklyInsight extends StatelessWidget {
  const WeeklyInsight({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}